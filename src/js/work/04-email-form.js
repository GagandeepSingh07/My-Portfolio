/* ==========================
   WORK PAGE EMAIL FORM
   ========================== */

// Initialize email form on work page using shared functionality
function initEmailForm() {
    // Use shared email form functionality
    if (typeof initSharedEmailForm === 'function') {
        initSharedEmailForm();
    }
}