/* ==========================
   HOMEPAGE RESUME LINKS
   ========================== */

// Initialize resume links on homepage using shared functionality
function initResumeLinks() {
    // Use shared resume links functionality
    if (typeof initSharedResumeLinks === 'function') {
        initSharedResumeLinks();
    }
}